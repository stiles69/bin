#======= COMMON ALIASES ADDED BY BRETT SALEMINK ======
alias zshconfig="vim ~/.zshrc"
alias ohmyzsh="cd ~/.oh-my-zsh"

# -> Prevents accidentally clobbering files.
alias mkdir='mkdir -p'

# Make sure try and attach to last session
alias tmux='tmux attach'

